<?php


/**
 * 1) CMS tempted to use is Anchor CMS
 */


/**
 * 2) Need to completly rebuild routing using controllers
 */

/**
 * 3) if user is not logged in as system the only editing they can do will be to the submissions
 * file there it can be rated and passed onto the system when it is completed
 *
 * if system is logged in editing the page source will be possible and this will update the
 * information on the page. (may delegate this to trusted users also possible to automate) 
 */